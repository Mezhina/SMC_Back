## Команды для запуска

### 2. Удаленная база данных (MySQL) - для продакшена

```bash
cd smc
mvn spring-boot:run -Dspring-boot.run.profiles=remote
```

### 3. Сборка проекта

```bash
cd smc
mvn clean compile
```
